/** Host loader entry for the browser-control browser implementation exported from `./client`. */
/** Host plugin body — no host-side behavior for the browser-control settings page. */
function apply() {}
export { apply };
