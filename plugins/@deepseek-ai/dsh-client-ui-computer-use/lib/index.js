/** Host loader entry for the computer-use settings browser implementation exported from `./client`. */
/** Host plugin body — no host-side behavior for the computer-use settings page. */
function apply() {}
export { apply };
