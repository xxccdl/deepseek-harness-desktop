/** Host loader entry for the scheduler browser implementation exported from `./client`. */
/** Host plugin body — no host-side behavior for the scheduler settings page. */
function apply() {}
export { apply };
