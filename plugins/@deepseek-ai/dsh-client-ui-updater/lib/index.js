/** Host loader entry for the updater browser implementation exported from `./client`. */
/** Host plugin body — no host-side behavior for the updater settings page. */
function apply() {}
export { apply };
