/** Host loader entry for the motion vocabulary exported from `./client`. */
/** Host plugin body — the motion layer is browser-only, so the host half does nothing. */
function apply() {}
export { apply };
