/** Host loader entry for the sidebar budget-bar browser implementation exported from `./client`. */
/** Host plugin body — no host-side behavior for the budget bar. */
function apply() {}
export { apply };
