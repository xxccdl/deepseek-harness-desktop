/** Host loader entry for the desktop-settings browser implementation exported from `./client`. */
/** Host plugin body — no host-side behavior for the desktop settings page. */
function apply() {}
export { apply };
