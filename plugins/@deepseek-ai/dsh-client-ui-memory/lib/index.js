/** Host loader entry for the memory-viewer browser implementation exported from `./client`. */
/** Host plugin body — no host-side behavior for the memory settings viewer. */
function apply() {}
export { apply };
