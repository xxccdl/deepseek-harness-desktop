/** Host loader entry for the quick-chat glass panel browser implementation exported from `./client`. */
/** Host plugin body — no host-side behavior for the quick-chat panel. */
function apply() {}
export { apply };
